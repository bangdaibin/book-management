<% provide(:title, 'All category') %>
<h1>All Category</h1>

  <%= will_paginate %>
<ul class="category">
      <%= render @categories %>
</ul>

<%= will_paginate %>
<% if logged_in? %>
	<% if current_user.admin? %>	
		<br><%= link_to "Add Category", new_category_path %></br>
		<br><%= link_to "Export to XLS", categories_path(format: "xls") %></br>
<% end %>
<% end %>
